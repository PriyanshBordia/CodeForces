#include <iostream>

using namespace std;

int main()
{
	string a;
	cin >> a;

	for (int i = s.size() - 1; i >= 0; i--)
	{
		if (int(s[i]) <= 8)
		{
			cout << int(a) + 8 - int(s[i]) << endl;
			break;
		}

		

	}
	return 0;
}