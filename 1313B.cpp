#include <iostream>

#define sc(x) scanf("%d", &x);
#define pf(x, y) printf("%d %d\n", x, y);

using namespace std;

int main()
{
	int t;
	sc(t);

	while (t--)
	{
		int n;
		sc(n);

		int x, y;
		sc(x)
		sc(y)

	}
	return 0;
}