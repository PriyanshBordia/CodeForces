#include <iostream>
#include <algorithm>
#include <cstring>
#include <math.h>

using namespace std;

int main()
{
	int t;
	cin >> t;

	while (t--)
	{
		int n, r;
		cin >> n >> r;

		long long int ans = 1;

		for (int i = 2; i <= n; ++i)
		{
		 	
		} 
	}
	
	return 0;
}